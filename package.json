{
    "name": "community-bot",
    "version": "3.9.40",
    "description": "All-in-One Discord Community Bot — ticket panel (multi-category), auto-responder, anti-spam, AFK, leveling, key-driven VIP role, self-role, temp voice, giveaway, scheduled announcements, embed builder, backup, warn, stats, poll, midman/escrow (3-party escrow deal, anyone can create a deal via a 3-step form, dual consent, member management inside the deal channel, additive fee) — fully configurable from Discord",
    "main": "index.js",
    "scripts": {
        "start": "node index.js",
        "dev": "nodemon index.js",
        "test": "node --test --test-concurrency=1 tests/unit/*.test.js",
        "test:unit": "node --test tests/unit/*.test.js",
        "lint": "eslint index.js src/",
        "lint:fix": "eslint index.js src/ --fix",
        "format": "prettier --write \"**/*.{js,json,md,yml}\" --ignore-path .gitignore",
        "format:check": "prettier --check \"**/*.{js,json,md,yml}\" --ignore-path .gitignore"
    },
    "devDependencies": {
        "@eslint/js": "^9.39.5",
        "eslint": "^9.0.0",
        "globals": "^17.11.0",
        "nodemon": "^3.1.14",
        "prettier": "^3.0.0"
    },
    "dependencies": {
        "discord.js": "^14.27.0",
        "dotenv": "^16.4.1"
    },
    "engines": {
        "node": ">=18"
    }
}
