{
  "name": "thor-en",
  "version": "4.0.0",
  "private": true,
  "description": "Thor-EN monorepo — ONE system, TWO independent modules: the Discord Bot (bot/) and the Web Dashboard (dashboard/), connected through the bot's DASH API (HTTP + shared token) and each with its own package.json. Root scripts orchestrate both; each module can also run standalone and is ready to be split into separate repos.",
  "scripts": {
    "setup": "./setup.sh",
    "start": "./start.sh",
    "dev": "./dev.sh",
    "bot:start": "npm --prefix bot start",
    "bot:dev": "npm --prefix bot run dev",
    "bot:test": "npm --prefix bot test",
    "bot:lint": "npm --prefix bot run lint",
    "dash:dev": "npm --prefix dashboard run dev",
    "dash:build": "npm --prefix dashboard run build",
    "dash:start": "npm --prefix dashboard run start",
    "dash:mock": "npm --prefix dashboard run mock"
  }
}
